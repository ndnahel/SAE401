{% extends 'base.html.twig' %}

{% block title %}{{ 'Définir un nouveau mot de passe'|trans }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/forms.css') }}">
{% endblock %}

{% block body %}
    <h1>{{ 'Définir un nouveau mot de passe'|trans }}</h1>

    {{ form_errors(form) }}

    {{ form_start(form) }}
    <button type="submit" class="btn">{{ 'Valider'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
